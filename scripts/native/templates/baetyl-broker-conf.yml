name: "{{.BrokerConfName}}"
namespace: "{{.Namespace}}"
system: true
labels:
  baetyl-app-name: "{{.BrokerAppName}}"
  baetyl-node-name: "{{.NodeName}}"
  baetyl-cloud-system: "true"
data:
  conf.yml: |-
    logger:
      level: debug
      encoding: console